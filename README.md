# ⚡️ LOE-Watcher: Монітор Графіку Відключень Світла (Львівобленерго)

Цей проєкт є автономним монітором, розробленим для відстеження оновлень у графіку погодинних відключень (ГПВ) на офіційному API-порталі **АТ «Львівобленерго» (loe.lviv.ua)** та миттєвого надсилання сповіщень із новим графіком у Telegram.

Він ідеально підходить для розгортання на **GitHub Actions** для повністю безкоштовного та надійного моніторингу 24/7.

## ✨ Основні функції

* **API-моніторинг:** Відстежує зміни безпосередньо в офіційному JSON-API, що забезпечує максимальну швидкість та надійність.
* **Хешування стану:** Використовує хешування JSON-відповіді для фіксації навіть найменших змін.
* **Сповіщення Telegram:** Надсилає сповіщення з **фотографією актуального графіку** та його **текстовою розшифровкою** у підписі.
* **Автоматичний Запуск:** Налаштований на автоматичну перевірку кожні 30 хвилин через GitHub Actions.
* **Резервний режим:** Надсилає лише текстове сповіщення, якщо фотографію скасували (наприклад, у випадку повної відміни ГПВ).

## 🚀 Встановлення та налаштування

Для розгортання цього монітора вам знадобиться лише обліковий запис GitHub та Telegram-бот.

### Крок 1: Отримання Telegram Bot Token та Chat ID

1.  **Створіть бота:** Напишіть @BotFather у Telegram і створіть нового бота. Збережіть отриманий **Bot Token**.
2.  **Отримайте Chat ID:** Додайте вашого нового бота до потрібного чату/каналу (або просто напишіть йому повідомлення). Використовуйте інший бот, наприклад @getidsbot, або просто перейдіть за адресою `https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates` у браузері, щоб знайти ваш **Chat ID** (він матиме вигляд `-123456789`).

### Крок 2: Налаштування GitHub Secrets

1.  Перейдіть до вашого репозиторію на GitHub.
2.  Натисніть **Settings** (Налаштування) → **Secrets and variables** (Секрети та змінні) → **Actions** (Дії).
3.  Натисніть **New repository secret** (Новий секрет репозиторію) і додайте два секрети:

| Ім'я Secret | Значення | Опис |
| :--- | :--- | :--- |
| `TELEGRAM_BOT_TOKEN` | Ваш Bot Token, отриманий від @BotFather | Токен для авторизації бота |
| `TELEGRAM_CHAT_ID` | Ваш Chat ID, куди надсилати сповіщення | ID цільового чату/каналу |

### Крок 3: Налаштування файлів

Переконайтеся, що ваш проєкт містить три основні файли:

1.  **`package.json`**: Містить необхідні залежності.

    ```json
    {
      "dependencies": {
        "axios": "^1.0.0",
        "form-data": "^4.0.0"
        // Можуть бути інші залежності
      }
    }
    ```

2.  **`index.js`**: Основна логіка моніторингу (повністю оновлений та функціональний код, наданий у діалозі).

3.  **`.github/workflows/monitor.yml`**: Файл робочого процесу GitHub Actions.

    ```yaml
    # ... (переконайтеся, що крок Install dependencies викликає npm install без параметрів)
    - name: Install dependencies
      run: npm install 
    # ...
    ```

### Крок 4: Запуск

1.  Запуште всі ваші файли до головної гілки (`main` або `master`).
2.  Перейдіть до вкладки **Actions** (Дії).
3.  Оберіть робочий процес **Monitor LOE Schedule**.
4.  Натисніть кнопку **Run workflow** (Запустити робочий процес) для ручної перевірки.

Перший запуск збереже поточний графік у файл `state.json` та надішле вам перше сповіщення. Наступні запуски (кожні 30 хвилин) надсилатимуть сповіщення лише тоді, коли графік дійсно зміниться.

## ⚙️ Технічні деталі

* **API Endpoint:** `https://api.loe.lviv.ua/api/menus?page=1&type=photo-grafic`
* **Мова:** JavaScript (Node.js)
* **Хостинг:** GitHub Actions
* **Залежності:** `axios`, `form-data`

## 🤝 Внесок

Цей проєкт був розроблений спільно з AI-помічником для розв'язання практичної проблеми моніторингу критичної інфраструктури. Якщо у вас є пропозиції щодо покращення логіки парсингу або розширення функціоналу, надсилайте Pull Requests!
